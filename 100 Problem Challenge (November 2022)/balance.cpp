#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int main(void) {
    vector<int> cows(12);
    for (int i = 0; i < 12; i++) {
        cin >> cows[i];
    }
    // split into 4 groups as evenly as possible
    vector<int> groups(4);
    
}