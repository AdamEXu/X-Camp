#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

// calculate number of ways to place n queens on an n x n board
int nqueens(int n) {
    
}

int main(void) {
    int n;
    cin >> n;
    cout << nqueens(n) << '\n';
}