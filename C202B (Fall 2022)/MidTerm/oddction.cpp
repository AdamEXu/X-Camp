#include <iostream>
#include <queue>
using namespace std;

int w, p, a, b;

int solvr(int index) {
    if (index < p) {
        return 0;
    } else if (index == p) {
        return 1;
    } else if ()
}

int main(void) {
    cin >> w >> p >> a >> b;
    cout << solvr(w) << endl;
}