#include <iostream>
#include <queue>
#include <vector>
#include <map>
#include <string>
using namespace std;

int main(void) {
    int n, m, a, b;
    cin >> n >> m >> a >> b;
    map<pair<int, int>, bool> points;
    
}